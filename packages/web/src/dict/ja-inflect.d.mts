// Type shim for ./ja-inflect.mjs (plain JS kept node-testable; tsc resolves via this file).
export function deinflectJa(token: string): string[];
export function isJaKanaFragment(token: string): boolean;
